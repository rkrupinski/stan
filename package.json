{
  "private": true,
  "workspaces": [
    "packages/stan",
    "packages/examples/*"
  ],
  "scripts": {
    "typecheck": "wsrun -p @rkrupinski/stan -c typecheck",
    "lint": "wsrun -p @rkrupinski/stan -c lint",
    "test": "wsrun -p @rkrupinski/stan -c test",
    "build": "wsrun -p @rkrupinski/stan -c build"
  },
  "devDependencies": {
    "wsrun": "5.2.4"
  }
}
